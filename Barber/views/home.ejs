<!DOCTYPE html>
<html>
<head>
    <title>Salone di Bellezza</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <header class="navbar">
      <a href="/" class="logo">
        <i class="fas fa-cut"></i>
        BarberApp
      </a>
    </header>
    <div class="container">
        <h1>Benvenuto nel nostro salone!</h1>
        <% if (user) { %>
            <p>Ciao <%= user.email || 'Utente' %>! <a href="/book">Prenota ora</a> | <a href="/logout">Logout</a></p>
        <% } else { %>
            <div class="auth-links">
                <a href="/login" class="btn">Login</a>
                <a href="/register" class="btn">Registrati</a>
                <a href="/auth/google" class="btn google-btn">Accedi con Google</a>
            </div>
        <% } %>
    </div>
</body>
</html>